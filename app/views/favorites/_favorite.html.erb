<% if favorited_by?(current_user) %>
  <%= link_to book.favorites.count,book_favorite_path(book),class: "fas fa-heart heart_red" %>
<% else %>
  <%= link_to book.favorites.count,book_favorite_path(book),method: :delete,class: "fas fa-heart heart_blue" %>
<% end %>